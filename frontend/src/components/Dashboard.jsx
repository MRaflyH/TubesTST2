import React, { useContext } from 'react';

const Dashboard = () => {
  return (
    <div>
      <h1>Welcome to the Dashboard</h1>
      <p>Use the navigation menu to explore the application.</p>
    </div>
  );
};

export default Dashboard;
